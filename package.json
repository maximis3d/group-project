{
  "name": "my-wellbeing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mywellbeing"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend --silent\" \"npm run dev --workspace mywellbeing --silent\"",
    "start": "npm run dev",
    "test": "jest"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
